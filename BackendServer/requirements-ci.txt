# Lightweight dependencies required by unit tests and feature extraction.
# Runtime-only Firebase, OpenAI, Ultralytics, and PyTorch packages are excluded
# so pull-request checks remain fast and do not require credentials or models.
boto3>=1.35,<2
joblib>=1.4,<2
# Newer MediaPipe wheels removed the legacy `mp.solutions` API used by main.py.
mediapipe==0.10.21
numpy>=1.26,<2
opencv-python-headless>=4.10,<5
pandas>=2.2,<3
Pillow>=11,<12
scikit-learn>=1.5,<1.7
scipy>=1.14,<2
tqdm>=4.67,<5
